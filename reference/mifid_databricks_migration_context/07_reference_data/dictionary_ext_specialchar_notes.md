# Dictionary_Ext_SpecialChar.csv Notes

`Dictionary_Ext_SpecialChar.csv` is the exported data for SQL Server `Dictionary.Ext_SpecialChar`.

It has no header row.

Column order:
1. `Key`
2. `Value`
3. `UpdateDate`

Used by:
- `dbo.SP_InstrumentMetaData_SpecialChar_Conversion`

Databricks target object:

```text
main.regtech_ops_stg.bi_output_regtechops_dictionary_ext_specialchar
```

All objects created in `main.regtech_ops_stg` must start with:

```text
bi_output_regtechops_
```
